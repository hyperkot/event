# the thing which is created when npm fails
/npm-debug.log

# obviously we don't want node_modules to be in the repo
/node_modules

# these are typesscript compilation products and temporary files
/src/**/*.js
/test/**/*.js
index.js
